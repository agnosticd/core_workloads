kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: keycloak
spec:
  host: sso.{{ _ocp4_workload_authentication_keycloak_base_domain }}
  to:
    kind: Service
    name: keycloak
    weight: 100
  port:
    targetPort: https
  tls:
    termination: reencrypt
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None